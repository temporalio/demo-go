package common

const (
	// WorkflowTaskList is the queue used by worker to pull workflow tasks
	WorkflowTaskList = "transfer"
	// ActivityTaskList is the queue used by worker to pull activity tasks
	ActivityTaskList = "transfer"
)
